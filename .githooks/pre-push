#!/usr/bin/env bash
set -euo pipefail

while read local_ref local_sha remote_ref remote_sha; do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "=== Push to main detected — deploying to itch.io ==="
    cd "$(dirname "$0")/.."
    exec scripts/deploy-itch.sh
  fi
done
